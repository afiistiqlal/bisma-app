import Layout from "@/components/Layout";
import React from "react";

type Props = {};

const paymentSimulation = (props: Props) => {
  const pageTitle = "Payment Simulation";
  return (
    <Layout title={pageTitle}>
      <div>paymentSimulation</div>
    </Layout>
  );
};

export default paymentSimulation;
